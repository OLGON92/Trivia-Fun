export default class Host {
    constructor(user, right) { 
        this.user = user;
        this.right = right;
    }
    moment()
    {
        point = false;
        if(this.user === this.right)
        {
            point = true;
        }
    return point;
    }
    
}